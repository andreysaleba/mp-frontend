function ListingImageCarouselDotsContainer({ id }: { id: number }) {
  return (
    <div className={`listing-image-carousel-dots-container listing-image-carousel-dots-container-${id}`}>123</div>
  );
}

export default ListingImageCarouselDotsContainer;